name: Python application

on: [push]

jobs:
  test:

    runs-on: linux

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v1
      with:
        python-version: 3.6
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Test cube_helper
      run: |
        python tests/test_cube_equaliser.py
        python tests/test_cube_loader.py
        python tests/test_cube_help.py
        
    - uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v1
      with:
        python-version: 2.7
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Test cube_helper
      run: |
        python tests/test_cube_equaliser.py
        python tests/test_cube_loader.py
        python tests/test_cube_help.py

